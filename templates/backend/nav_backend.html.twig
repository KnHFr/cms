{% block body %}
<body>

    <div class="row">
        <div class="col-2">
            <h1>MENU</h1>
            <ul class="nav nav-pills">
                <li class="nav-item col-12">
                    <a class="nav-link" href="{{ path('backend_parameter_head') }}">En-tête</a>
                </li>
                <li class="nav-item col-12">
                    <a class="nav-link" href="{{ path('backend_parameter_foot') }}">Pied de page</a>
                </li>
                <li class="nav-item col-12">
                    <a class="nav-link" href="{{ path('backend_article_index') }}">Articles</a>
                </li>
                {# <li class="nav-item col-12">
                    <a class="nav-link {{ app.request.get('_route') == 'backend_parameter_head' ? 'active bg-secondary' }}" href="{{ path('backend_parameter_head') }}">En-tête</a>
                </li>
                <li class="nav-item col-12">
                    <a class="nav-link {{ app.request.get('_route') == 'backend_parameter_foot' ? 'active bg-secondary' }}" href="{{ path('backend_parameter_foot') }}">Pied de page</a>
                </li>
                <li class="nav-item col-12">
                    <a class="nav-link {{ app.request.get('_route') == 'backend_article_index' ? 'active bg-secondary' }}" href="{{ path('backend_article_index') }}">Articles</a>
                </li> #}
            </ul>
        </div>
<main>

{% endblock %}